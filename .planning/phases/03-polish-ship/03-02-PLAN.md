---
phase: 03-polish-ship
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified: []
autonomous: false
requirements:
  - ANIM-03

must_haves:
  truths:
    - "Lighthouse on the live corsiriccardo.github.io URL scores Performance >= 90"
    - "LCP is < 1.5s on the live URL measured under the Mobile preset"
    - "CLS = 0 on the live URL — no layout shift from font swap, texture, or scroll reveal transforms"
    - "TBT = 0 on the live URL — no long tasks block the main thread during page load"
    - "The page is fully readable on a physical or emulated mobile device at 375px viewport width — all text visible, no horizontal scroll, no clipped content"
  artifacts:
    - path: ".planning/phases/03-polish-ship/03-02-SUMMARY.md"
      provides: "Lighthouse scores recorded, any remediation applied"
      contains: "Performance"
  key_links:
    - from: "live URL (https://corsiriccardo.github.io)"
      to: "Lighthouse Mobile audit"
      via: "Chrome DevTools → Lighthouse tab → Mobile preset → Generate report"
      pattern: "n/a — external tool"
---

<objective>
Verify that the Phase 3 animation work ships correctly on the live GitHub Pages URL and meets all
Lighthouse performance targets. This plan:

1. Pushes the Phase 3 changes to master so GitHub Pages deploys them.
2. Waits for the deploy, then verifies scroll reveal animations work on the live site.
3. Runs a Lighthouse Mobile audit on https://corsiriccardo.github.io and records scores.
4. If any metric fails, applies the targeted fix from the research remediation list.
5. Verifies the page is readable at 375px viewport width.

Purpose: Satisfy ANIM-03 — the final shippability gate for the entire project.

Output: Live site with passing Lighthouse scores. Scores recorded in the phase summary.
</objective>

<execution_context>
@E:\Users\riky\.claude\get-shit-done\workflows\execute-plan.md
@E:\Users\riky\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-polish-ship/03-RESEARCH.md
@.planning/phases/03-polish-ship/03-01-SUMMARY.md

<interfaces>
<!-- Lighthouse metric targets for this audit (from ROADMAP Phase 3 success criteria) -->
<!-- Run the audit in Chrome DevTools incognito, Mobile preset — NOT Desktop, NOT localhost -->

Metric targets (all must pass):
  Performance score: >= 90
  LCP: < 1.5s  (Lighthouse "good" threshold is < 2.5s; we target tighter)
  CLS: 0        (target is exactly 0; Lighthouse "good" is < 0.1)
  TBT: 0ms      (target is exactly 0ms; any value > 0ms indicates a long task)

Lighthouse audit steps (verbatim from research):
  1. Open https://corsiriccardo.github.io in Chrome (LIVE URL — not localhost)
  2. Use incognito window with extensions disabled
  3. DevTools (F12) → Lighthouse tab
  4. Select: Mobile (NOT Desktop)
  5. Categories: Performance only
  6. Click "Analyze page load"
  7. Record: Performance score, LCP value, CLS value, TBT value, LCP element

Remediation reference (if metrics fail):
  - CLS > 0 from font swap: Add size-adjust to @font-face fallback in css/base.css
    (Only apply if Lighthouse shows CLS > 0; do not pre-emptively add)
  - LCP > 1.5s: Check LCP element; if it's the H1, verify font preload tags are present
    in index.html (they should be from Phase 1 — both woff2 preloads in <head>)
  - TBT > 0: Check for synchronous scripts in <head>; there should be none

Deploy verification steps:
  After git push master:
  1. Visit https://github.com/corsiriccardo/corsiriccardo.github.io/actions to confirm
     the Pages deploy action completes (usually < 2 minutes)
  2. Visit https://corsiriccardo.github.io — hard reload (Ctrl+Shift+R) to clear cache
  3. Scroll through all sections — confirm data-reveal animations fire on scroll
  4. Check #hero — confirm it renders immediately at full opacity with no animation delay

Mobile 375px verification:
  In Chrome DevTools → Device Toolbar (Ctrl+Shift+M) → set to 375 x 667 (iPhone SE)
  Check: no horizontal scrollbar, all text readable (not clipped), nav hamburger works,
  project cards stack vertically, contact buttons wrap or stack correctly
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push Phase 3 changes to master and wait for GitHub Pages deploy</name>
  <read_first>
    - .planning/phases/03-polish-ship/03-01-SUMMARY.md — confirm Plan 01 completed successfully before pushing
  </read_first>
  <what-built>
    Plan 03-01 implemented scroll reveal (css/animations.css fill, js/reveal.js, data-reveal attributes
    in index.html). Those changes need to be committed and pushed so GitHub Pages deploys them.
  </what-built>
  <how-to-verify>
    1. Stage and commit the Phase 3 changes if not already committed:
       ```
       git add css/animations.css js/reveal.js index.html
       git commit -m "feat(03): add scroll reveal animations and hover transitions"
       ```
    2. Push to master:
       ```
       git push origin master
       ```
    3. Visit https://github.com/corsiriccardo/corsiriccardo.github.io/actions
       Wait for the "pages build and deployment" action to show a green checkmark.
       (Typically completes within 1-2 minutes of the push.)
    4. Once the deploy action is green, open https://corsiriccardo.github.io in a new
       Chrome incognito window. Hard-reload (Ctrl+Shift+R).
    5. Scroll down slowly — verify that the projects, about, timeline, and contact sections
       animate in (fade up from slightly below). The hero should appear immediately.
    6. Type "approved" below when the live site shows the animations working correctly.
  </how-to-verify>
  <resume-signal>Type "approved" once animations are confirmed working on the live URL, or describe any issue observed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run Lighthouse Mobile audit on live URL and record scores</name>
  <read_first>
    - .planning/phases/03-polish-ship/03-RESEARCH.md "Lighthouse Performance Analysis for This Stack" — expected score ranges and failure mode descriptions
    - .planning/phases/03-polish-ship/03-RESEARCH.md "Pitfall 5: Lighthouse in non-incognito mode" — MUST use incognito
    - .planning/phases/03-polish-ship/03-RESEARCH.md "Pitfall 6: LCP element changes after font-display: swap" — if CLS > 0
    - .planning/phases/03-polish-ship/03-RESEARCH.md "Pitfall 7: Lighthouse run against localhost" — MUST use live URL
  </read_first>
  <what-built>
    The live site now has all Phase 3 animations deployed. This checkpoint runs the final
    Lighthouse audit to verify performance targets are met.
  </what-built>
  <how-to-verify>
    LIGHTHOUSE AUDIT (run in Chrome incognito, Mobile preset):

    1. Open https://corsiriccardo.github.io in Chrome incognito (Ctrl+Shift+N)
       Disable any extensions if prompted.
    2. Open DevTools (F12) → click the ">>" tab overflow → Lighthouse
    3. Settings: Device = Mobile, Categories = check only "Performance"
    4. Click "Analyze page load" and wait for the report (30-60 seconds)
    5. Record these values from the report:
       - Overall Performance score (the large number at top)
       - LCP: shown in "Metrics" section
       - CLS: shown in "Metrics" section
       - TBT: shown in "Metrics" section
       - LCP element: shown in "Diagnostics" → "Largest Contentful Paint element"

    PASS criteria (all must be true to type "approved"):
    - Performance score >= 90
    - LCP < 1.5s
    - CLS = 0 (or 0.000 — any non-zero value is a failure)
    - TBT = 0ms (or 0 — any non-zero value needs investigation)

    IF CLS > 0 (font-swap layout shift):
    - Open css/base.css
    - Find the @font-face declarations for Space Grotesk and Inter
    - Add `size-adjust: 100%;` to each @font-face block as a starting value
    - Commit and push; wait for redeploy; re-run Lighthouse
    - (This is the only expected remediation based on research analysis)

    IF TBT > 0:
    - Open DevTools → Performance tab → record a fresh page load
    - Look for any long task (red bar > 50ms) in the main thread
    - Report the task details — likely a third-party script that shouldn't be present

    IF Performance < 90 with LCP or TBT the culprit:
    - Check that both font preload tags are still present in index.html <head>:
      `<link rel="preload" href="assets/fonts/space-grotesk-400.woff2" ...>`
      `<link rel="preload" href="assets/fonts/inter-400.woff2" ...>`

    MOBILE 375px VERIFICATION (run after Lighthouse):
    1. DevTools → Device Toolbar (Ctrl+Shift+M) → iPhone SE (375 x 667)
    2. Scroll through the entire page. Verify:
       - No horizontal scrollbar
       - All text is fully visible (not clipped at edges)
       - Nav hamburger button appears and opens the mobile menu
       - Project cards stack in a single column (not overflowing)
       - Contact buttons stack vertically on mobile (they should wrap on narrow viewport)
       - Footer copyright visible at the bottom

    Type below: "approved: Performance=XX, LCP=X.Xs, CLS=0, TBT=0" replacing XX with actual scores.
    Or describe the failing metric and the remediation applied.
  </how-to-verify>
  <resume-signal>Type "approved: Performance=XX, LCP=X.Xs, CLS=0, TBT=0" with actual scores, or describe any failing metric and remediation applied</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live URL → Chrome Lighthouse | Lighthouse fetches the live page from GitHub Pages CDN; no user input or authentication involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Lighthouse audit — reads live page | accept | Lighthouse only reads publicly-available static assets; no secrets, no auth tokens, no PII in the audit scope |
| T-03-05 | Denial of Service | GitHub Pages deploy pipeline | accept | Deploy is triggered by git push; GitHub Pages is a managed service with documented SLA; no mitigation needed for a personal portfolio |
</threat_model>

<verification>
Lighthouse scores are the verification. The plan is complete when:
- Performance score: reported >= 90
- LCP: reported < 1.5s
- CLS: reported = 0
- TBT: reported = 0ms
- Mobile 375px: no horizontal scroll, all content visible and readable

These are recorded in the phase summary for permanent reference.
</verification>

<success_criteria>
1. Live site at https://corsiriccardo.github.io shows scroll reveal animations working (below-fold sections animate in on scroll; hero renders immediately)
2. Lighthouse Mobile audit on the live URL returns: Performance >= 90, LCP < 1.5s, CLS = 0, TBT = 0
3. Page is fully readable at 375px viewport width with no horizontal overflow and no clipped content
4. Actual Lighthouse scores are recorded in 03-02-SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-ship/03-02-SUMMARY.md` using the summary template.

Include in the summary:
- Actual Lighthouse scores: Performance, LCP, CLS, TBT
- LCP element identified in the report
- Whether any remediation was applied (e.g. size-adjust for CLS) and what the before/after scores were
- Mobile 375px result (pass/fail with any noted issues)
- Phase 3 overall status: complete or needs further work
</output>
