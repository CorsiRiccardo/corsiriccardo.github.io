---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - index.html
  - js/nav.js
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - LAYOUT-05

must_haves:
  truths:
    - "index.html is a complete rewrite — no references to old src/styles.css, src/app.js, or FontAwesome CDN"
    - "Font preload links appear before CSS links in <head>"
    - "All 6 CSS files are linked in layer order: tokens, reset, base, layout, components, animations"
    - "OG meta tags are present with absolute URLs and correct copy"
    - "Semantic structure: header > nav, main > 5 sections with aria-label, footer"
    - "Skip-to-content link is the first child of <body>"
    - "Hamburger button has aria-expanded and aria-label"
    - "Nav.js implements Intersection Observer with rootMargin -64px 0px -66% 0px"
    - "Nav.js implements hamburger toggle with aria-expanded update"
    - "All links in HTML reference lowercase asset paths"
  artifacts:
    - path: "index.html"
      provides: "Semantic HTML shell with OG meta, font preloads, CSS links, nav, 5 section placeholders"
      contains: "og:title"
    - path: "js/nav.js"
      provides: "ES module: Intersection Observer active-link + hamburger toggle"
      contains: "IntersectionObserver"
  key_links:
    - from: "index.html"
      to: "css/tokens.css"
      via: "<link rel=stylesheet>"
      pattern: "href=\"css/tokens\\.css\""
    - from: "index.html"
      to: "assets/fonts/"
      via: "<link rel=preload>"
      pattern: "rel=\"preload\".*as=\"font\""
    - from: "index.html"
      to: "js/nav.js"
      via: "<script type=module>"
      pattern: "type=\"module\".*js/nav\\.js"
    - from: "js/nav.js"
      to: "main section[id]"
      via: "IntersectionObserver.observe()"
      pattern: "querySelectorAll.*main.*section"
---

<objective>
Rewrite index.html as the semantic HTML shell and create js/nav.js as the Intersection Observer navigation script.

Purpose: Wire all the CSS and asset files from Plans 01 and 02 into a working page. index.html must link to all CSS files in layer order, preload all fonts, declare all OG meta, and provide the semantic landmark structure for Phase 2 content.

Output: index.html (full rewrite), js/nav.js (ES module)
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-01-SUMMARY.md
@.planning/phases/01-foundation-shell/01-02-SUMMARY.md

<interfaces>
<!-- Complete HTML shell and nav.js are documented in RESEARCH.md Open Questions 5 and 7.
     Executor extracts exact copy from UI-SPEC Copywriting Contract. -->

OG meta values (from UI-SPEC Copywriting Contract):
  title:       Riccardo Corsi — Software Developer
  description: Software developer specialising in game systems and backend tools. View my projects and experience.
  og:image:    https://corsiriccardo.github.io/assets/og-image.png  (MUST be absolute URL)
  og:url:      https://corsiriccardo.github.io
  og:type:     website

Nav labels (from UI-SPEC Copywriting Contract):
  Home | Projects | About | Experience | Contact
  (href values: #hero #projects #about #timeline #contact)

CSS link order (must match @layer declaration):
  css/tokens.css → css/reset.css → css/base.css → css/layout.css → css/components.css → css/animations.css

Intersection Observer rootMargin: '-64px 0px -66% 0px'
  (64px = nav height; -66% bottom = section active only in top 34% of viewport)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.html — complete semantic shell</name>
  <files>index.html</files>
  <read_first>
    - index.html (READ current file first — understand what old references to remove)
    - .planning/phases/01-foundation-shell/01-RESEARCH.md (section "HTML5 Accessibility: Skeleton Considerations" — complete shell with all attributes)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Open Graph & Meta Contract, Copywriting Contract, Navigation Contract)
  </read_first>
  <action>
Replace the entire contents of index.html. The new file is a complete rewrite — do NOT preserve any links or scripts from the old file (the old file references FontAwesome CDN and src/styles.css which must be removed entirely).

Write the following structure. Every attribute, value, and copy string must exactly match:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Software developer specialising in game systems and backend tools. View my projects and experience.">

  <!-- Open Graph — all values are exact; og:image MUST be absolute URL -->
  <meta property="og:title" content="Riccardo Corsi — Software Developer">
  <meta property="og:description" content="Software developer specialising in game systems and backend tools. View my projects and experience.">
  <meta property="og:image" content="https://corsiriccardo.github.io/assets/og-image.png">
  <meta property="og:url" content="https://corsiriccardo.github.io">
  <meta property="og:type" content="website">

  <title>Riccardo Corsi — Software Developer</title>

  <!-- Font preloads — MUST appear before CSS links to prevent double fetch.
       crossorigin attribute is required even for same-origin fonts (browser CORS behavior). -->
  <link rel="preload" href="assets/fonts/cormorant-garamond-400.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="assets/fonts/cormorant-garamond-600.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="preload" href="assets/fonts/lora-400.woff2" as="font" type="font/woff2" crossorigin>

  <!-- CSS — linked in @layer declaration order from tokens.css -->
  <link rel="stylesheet" href="css/tokens.css">
  <link rel="stylesheet" href="css/reset.css">
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/layout.css">
  <link rel="stylesheet" href="css/components.css">
  <link rel="stylesheet" href="css/animations.css">

  <!-- Favicons — SVG for modern browsers, ICO fallback for old browsers -->
  <link rel="icon" href="assets/favicon.ico" sizes="any">
  <link rel="icon" href="assets/favicon.svg" type="image/svg+xml">
</head>
<body>
  <!-- Skip link: first element in body for keyboard navigation accessibility -->
  <a href="#main-content" class="skip-link">Skip to main content</a>

  <header>
    <nav aria-label="Main navigation">
      <ul>
        <li><a href="#hero" class="nav-link">Home</a></li>
        <li><a href="#projects" class="nav-link">Projects</a></li>
        <li><a href="#about" class="nav-link">About</a></li>
        <li><a href="#timeline" class="nav-link">Experience</a></li>
        <li><a href="#contact" class="nav-link">Contact</a></li>
      </ul>
      <button class="nav__hamburger" aria-label="Toggle navigation" aria-expanded="false">
        <svg width="24" height="24" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
          <rect y="4" width="24" height="2" fill="currentColor"/>
          <rect y="11" width="24" height="2" fill="currentColor"/>
          <rect y="18" width="24" height="2" fill="currentColor"/>
        </svg>
      </button>
    </nav>
  </header>

  <main id="main-content">
    <section id="hero" aria-label="Introduction">
      <!-- Phase 2 content: hero name, title, tagline, CTA -->
    </section>

    <section id="projects" aria-label="Projects">
      <!-- Phase 2 content: project cards loaded from data/projects.json -->
    </section>

    <section id="about" aria-label="About">
      <!-- Phase 2 content: about copy, stack -->
    </section>

    <section id="timeline" aria-label="Work experience">
      <!-- Phase 2 content: timeline entries -->
    </section>

    <section id="contact" aria-label="Contact">
      <!-- Phase 2 content: email and LinkedIn CTAs -->
    </section>
  </main>

  <footer>
    <!-- Phase 2 content: footer copy -->
  </footer>

  <!-- nav.js must be type="module" — uses ES module syntax (import/export not used here
       but module semantics apply: deferred execution, strict mode, no global leak) -->
  <script type="module" src="js/nav.js"></script>
</body>
</html>
```

CRITICAL checks after writing:
1. Confirm there is NO reference to `src/styles.css`, `src/app.js`, `app.js`, FontAwesome, or Poppins anywhere in the new file.
2. Confirm the `<link rel="preload">` tags appear BEFORE the `<link rel="stylesheet">` tags in `<head>`.
3. Confirm `og:image` uses the full absolute URL `https://corsiriccardo.github.io/assets/og-image.png`.
  </action>
  <verify>
    <automated>grep -c "og:" /e/Projects/corsiriccardo.github.io/index.html && grep -n "rel=\"preload\"" /e/Projects/corsiriccardo.github.io/index.html && grep -n "FontAwesome\|poppins\|src/styles\|src/app" /e/Projects/corsiriccardo.github.io/index.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<html lang="en">`
    - index.html contains `<meta charset="UTF-8">`
    - index.html contains `<meta name="viewport" content="width=device-width, initial-scale=1">`
    - index.html contains `<meta property="og:title" content="Riccardo Corsi — Software Developer">`
    - index.html contains `<meta property="og:image" content="https://corsiriccardo.github.io/assets/og-image.png">`
    - index.html contains `<meta property="og:url" content="https://corsiriccardo.github.io">`
    - index.html contains `<meta property="og:type" content="website">`
    - index.html contains exactly 3 `rel="preload"` font preload links, all with `crossorigin` attribute
    - index.html contains `href="css/tokens.css"` as the first stylesheet link
    - index.html contains all 6 CSS links in order: tokens, reset, base, layout, components, animations
    - index.html contains `<a href="#main-content" class="skip-link">` as first element inside `<body>`
    - index.html contains `<nav aria-label="Main navigation">`
    - index.html contains `<button class="nav__hamburger" aria-label="Toggle navigation" aria-expanded="false">`
    - index.html contains `<main id="main-content">`
    - index.html contains exactly 5 `<section>` elements: hero, projects, about, timeline, contact — each with `aria-label`
    - index.html contains `<script type="module" src="js/nav.js">`
    - index.html does NOT contain "FontAwesome", "poppins", "src/styles", or "src/app"
    - All href values for assets use lowercase paths (no uppercase in any href)
  </acceptance_criteria>
  <done>index.html is a complete rewrite with all Phase 1 HTML requirements met: OG meta, font preloads, 6 CSS links in layer order, semantic landmark structure, skip link, hamburger button, 5 section placeholders with aria-labels, nav.js module script.</done>
</task>

<task type="auto">
  <name>Task 2: Create js/nav.js — Intersection Observer + hamburger toggle</name>
  <files>js/nav.js</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-RESEARCH.md (section "Intersection Observer Pattern for Active Nav Links" — contains complete nav.js code with hamburger toggle)
    - index.html (verify section IDs and class names match what nav.js selects)
  </read_first>
  <action>
Create the js/ directory and write js/nav.js as an ES module. The file must implement exactly:

1. **Active-link Intersection Observer** — observes `main section[id]` elements. When a section enters the top 34% of the viewport (below the 64px nav), it adds `.nav-link--active` to the matching `nav a[href="#sectionId"]` link and removes it from all others.

2. **Hamburger toggle** — clicking `.nav__hamburger` toggles `.nav--open` on the `nav` element and updates `aria-expanded` attribute.

3. **Mobile nav close on link click** — clicking any nav link while the mobile menu is open closes the menu.

```javascript
// js/nav.js
// ES module — active-link via Intersection Observer + hamburger toggle
// Matches section IDs: hero, projects, about, timeline, contact

const NAV_HEIGHT = 64; // matches --nav-height token (64px)

const navLinks = document.querySelectorAll('nav a[href^="#"]');
const hamburger = document.querySelector('.nav__hamburger');
const nav = document.querySelector('nav');

// --- Intersection Observer: active nav link ---
// rootMargin: subtract nav height from top so section fires below nav;
// -66% bottom means section must be in the top 34% of visible area to count as active.
const sectionObserver = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      const id = entry.target.id;
      navLinks.forEach((link) => link.classList.remove('nav-link--active'));
      const activeLink = document.querySelector(`nav a[href="#${id}"]`);
      if (activeLink) activeLink.classList.add('nav-link--active');
    });
  },
  {
    rootMargin: `-${NAV_HEIGHT}px 0px -66% 0px`,
    threshold: 0,
  }
);

document.querySelectorAll('main section[id]').forEach((section) => {
  sectionObserver.observe(section);
});

// --- Hamburger toggle ---
if (hamburger && nav) {
  hamburger.addEventListener('click', () => {
    nav.classList.toggle('nav--open');
    const isOpen = nav.classList.contains('nav--open');
    hamburger.setAttribute('aria-expanded', String(isOpen));
  });
}

// --- Close mobile menu when a nav link is clicked ---
navLinks.forEach((link) => {
  link.addEventListener('click', () => {
    nav.classList.remove('nav--open');
    if (hamburger) hamburger.setAttribute('aria-expanded', 'false');
  });
});
```

Note: No import/export statements needed — this file uses no external modules. `type="module"` in the HTML script tag gives deferred execution (runs after DOM is parsed) and strict mode automatically. Do not add `defer` attribute — module scripts are already deferred.
  </action>
  <verify>
    <automated>grep -n "IntersectionObserver" /e/Projects/corsiriccardo.github.io/js/nav.js && grep -n "nav-link--active" /e/Projects/corsiriccardo.github.io/js/nav.js && grep -n "aria-expanded" /e/Projects/corsiriccardo.github.io/js/nav.js && grep -n "rootMargin" /e/Projects/corsiriccardo.github.io/js/nav.js</automated>
  </verify>
  <acceptance_criteria>
    - js/nav.js exists
    - js/nav.js contains `new IntersectionObserver(`
    - js/nav.js contains `rootMargin: \`-${NAV_HEIGHT}px 0px -66% 0px\`` (or the literal string `-64px 0px -66% 0px`)
    - js/nav.js contains `nav-link--active`
    - js/nav.js contains `classList.remove('nav-link--active')` (removes from all before adding)
    - js/nav.js contains `aria-expanded`
    - js/nav.js contains `nav--open`
    - js/nav.js contains `querySelectorAll('main section[id]')` (or equivalent selector targeting main sections)
    - js/nav.js does NOT contain any `import` or `export` statements (standalone module, no dependencies)
  </acceptance_criteria>
  <done>js/nav.js created as ES module with Intersection Observer active-link detection (rootMargin -64px 0px -66% 0px), hamburger toggle updating aria-expanded, and mobile menu close on link click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML → browser | Static HTML, no user input processed |
| nav.js → DOM | JS reads section IDs from DOM; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | og:image absolute URL | accept | Public portfolio; absolute URL is correct and intentional |
| T-03-02 | Information Disclosure | HTML source | accept | Portfolio is public; no sensitive information in HTML |
| T-03-03 | Spoofing | nav anchor links | accept | All href targets are same-page anchors; no external navigation risks |
</threat_model>

<verification>
After both tasks complete, run these checks:

```bash
# OG tags present with correct values
grep "og:title\|og:image\|og:url\|og:type" /e/Projects/corsiriccardo.github.io/index.html

# Font preloads present with crossorigin
grep "preload.*font\|font.*preload" /e/Projects/corsiriccardo.github.io/index.html

# All 6 CSS files linked
grep "rel=\"stylesheet\"" /e/Projects/corsiriccardo.github.io/index.html

# No old file references
grep "FontAwesome\|poppins\|src/styles\|src/app" /e/Projects/corsiriccardo.github.io/index.html

# nav.js Intersection Observer present
grep "IntersectionObserver\|rootMargin" /e/Projects/corsiriccardo.github.io/js/nav.js

# Confirm no uppercase in asset href paths
grep -i "assets" /e/Projects/corsiriccardo.github.io/index.html | grep -E '[A-Z]'
```

Expected: OG tags with correct values; 3 preload links with crossorigin; 6 stylesheet links; zero matches for old references; IntersectionObserver and rootMargin in nav.js; zero uppercase asset paths.
</verification>

<success_criteria>
- index.html is a complete rewrite with no references to old files or CDN dependencies
- All 5 OG meta properties present with exact copy from UI-SPEC
- og:image URL is absolute: https://corsiriccardo.github.io/assets/og-image.png
- 3 font preload links, each with crossorigin, appearing before CSS links
- 6 CSS stylesheet links in @layer order
- Semantic structure: header > nav with aria-label, main#main-content with 5 aria-labeled sections, footer
- Skip-to-content link as first body child
- Hamburger button with aria-expanded="false" and aria-label
- js/nav.js implements IO with rootMargin -64px 0px -66% 0px; hamburger toggle; mobile menu close
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md`
</output>
