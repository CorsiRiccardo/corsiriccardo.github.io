---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/download-fonts.js
  - scripts/generate-texture.js
  - scripts/generate-og-image.js
  - assets/fonts/cormorant-garamond-400.woff2
  - assets/fonts/cormorant-garamond-600.woff2
  - assets/fonts/lora-400.woff2
  - assets/textures/paper-grain.png
  - assets/og-image.png
  - assets/favicon.svg
  - assets/favicon.ico
autonomous: false
requirements:
  - FOUND-02
  - FOUND-03
  - LAYOUT-04

user_setup:
  - service: favicon-ico
    why: "Converting favicon.svg to favicon.ico requires either npx svg-to-ico (auto) or realfavicongenerator.net (manual fallback if npx fails)"
    env_vars: []
    dashboard_config:
      - task: "If npx svg-to-ico fails: upload assets/favicon.svg to realfavicongenerator.net, download the .ico file, save as assets/favicon.ico"
        location: "https://realfavicongenerator.net"

must_haves:
  truths:
    - "Three WOFF2 font files exist at assets/fonts/ with correct lowercase names"
    - "Each WOFF2 file is a valid font (> 10 KB)"
    - "Paper grain PNG exists at assets/textures/paper-grain.png and is 200x200px"
    - "OG image exists at assets/og-image.png and is 1200x630px"
    - "favicon.svg exists at assets/favicon.svg with warm cream background and RC initials"
    - "favicon.ico exists at assets/favicon.ico"
    - "All asset filenames are lowercase with no uppercase characters"
  artifacts:
    - path: "assets/fonts/cormorant-garamond-400.woff2"
      provides: "Cormorant Garamond regular weight font"
      min_bytes: 10000
    - path: "assets/fonts/cormorant-garamond-600.woff2"
      provides: "Cormorant Garamond semibold weight font"
      min_bytes: 10000
    - path: "assets/fonts/lora-400.woff2"
      provides: "Lora regular weight font"
      min_bytes: 10000
    - path: "assets/textures/paper-grain.png"
      provides: "200x200px warm noise tile for paper texture"
    - path: "assets/og-image.png"
      provides: "1200x630px OG social preview image"
    - path: "assets/favicon.svg"
      provides: "SVG favicon with RC initials"
      contains: "RC"
    - path: "assets/favicon.ico"
      provides: "ICO favicon fallback"
  key_links:
    - from: "assets/fonts/"
      to: "css/base.css"
      via: "@font-face url() references"
      pattern: "cormorant-garamond-400\\.woff2"
    - from: "assets/textures/paper-grain.png"
      to: "css/base.css"
      via: "body::before background-image"
      pattern: "paper-grain\\.png"
---

<objective>
Generate and download all binary assets required by Phase 1: three WOFF2 font files, paper grain PNG, OG image PNG, and favicon files.

Purpose: These assets are referenced by CSS (font-face, body::before) and HTML (preload, og:image, favicon links). They must exist before index.html can be loaded without 404s.

Output: assets/fonts/ (3 WOFF2), assets/textures/paper-grain.png, assets/og-image.png, assets/favicon.svg, assets/favicon.ico, scripts/ (3 Node scripts)
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md

<interfaces>
<!-- Exact download URLs and generation parameters — no look-up needed -->

Font download URLs (verified 2026-04-21):
  cormorant-garamond-400.woff2:
    https://fonts.gstatic.com/s/cormorantgaramond/v21/co3umX5slCNuHLi8bLeY9MK7whWMhyjypVO7abI26QOD_v86KnTOig.woff2
    Expected size: ~22,876 bytes

  cormorant-garamond-600.woff2:
    https://fonts.gstatic.com/s/cormorantgaramond/v21/co3umX5slCNuHLi8bLeY9MK7whWMhyjypVO7abI26QOD_iE9KnTOig.woff2
    Expected size: ~23,396 bytes

  lora-400.woff2:
    https://fonts.gstatic.com/s/lora/v37/0QI6MX1D_JOuGQbT0gvTJPa787weuxJBkq0.woff2
    Expected size: ~21,148 bytes

OG image spec:
  Size: 1200x630px
  Background: #F5F0E8 (warm cream)
  Text line 1: "Riccardo Corsi" — center, large, dark (#2C2416)
  Text line 2: "Software Developer" — center, smaller, secondary (#6B5D4F)
  Accent line: #8B6914 horizontal line below text
  Output: assets/og-image.png

Favicon spec:
  SVG: 32x32 viewBox, cream bg #F5F0E8, rounded rect, "RC" text in Georgia serif #2C2416
  ICO: multi-resolution (16x16, 32x32, 48x48) — generate from SVG
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run font download script + texture generation script</name>
  <files>scripts/download-fonts.js, scripts/generate-texture.js, assets/fonts/cormorant-garamond-400.woff2, assets/fonts/cormorant-garamond-600.woff2, assets/fonts/lora-400.woff2, assets/textures/paper-grain.png</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-RESEARCH.md (sections "Font WOFF2 Files: How to Obtain" and "Paper Grain PNG: How to Generate" — contain the complete Node.js scripts verbatim)
  </read_first>
  <action>
Create the scripts/ directory. Write two Node.js scripts using the exact code from RESEARCH.md, then run them.

**scripts/download-fonts.js** — Uses Node built-in https module, no npm deps:
The script downloads 3 WOFF2 files from Google Fonts CDN to assets/fonts/. Copy the complete script exactly from the RESEARCH.md "Font WOFF2 Files: How to Obtain" section. The three URLs and filenames are:
  - cormorant-garamond-400.woff2 from fonts.gstatic.com/s/cormorantgaramond/v21/co3umX5slCNuHLi8bLeY9MK7whWMhyjypVO7abI26QOD_v86KnTOig.woff2
  - cormorant-garamond-600.woff2 from fonts.gstatic.com/s/cormorantgaramond/v21/co3umX5slCNuHLi8bLeY9MK7whWMhyjypVO7abI26QOD_iE9KnTOig.woff2
  - lora-400.woff2 from fonts.gstatic.com/s/lora/v37/0QI6MX1D_JOuGQbT0gvTJPa787weuxJBkq0.woff2
Script creates assets/fonts/ directory with fs.mkdirSync({ recursive: true }).
Script uses https.get() and res.pipe(fs.createWriteStream(dest)).

IMPORTANT: The script uses async HTTP — add an event listener on res 'end' to log each completion. Wait for all three downloads to complete before verifying file sizes.

**scripts/generate-texture.js** — Pure Node, no npm deps (uses zlib + Buffer):
Copy the complete script exactly from the RESEARCH.md "Paper Grain PNG: How to Generate" section. Key parameters:
  - 200x200px PNG, RGB color type 2
  - Warm bias: R = base + noise + 8, G = base + noise + 2, B = base + noise - 6
  - noise range: -40 to +40
  - Output: assets/textures/paper-grain.png

Run both scripts:
```bash
node /e/Projects/corsiriccardo.github.io/scripts/download-fonts.js
node /e/Projects/corsiriccardo.github.io/scripts/generate-texture.js
```

After running, verify file sizes to confirm successful download/generation.
  </action>
  <verify>
    <automated>ls -la /e/Projects/corsiriccardo.github.io/assets/fonts/ && ls -la /e/Projects/corsiriccardo.github.io/assets/textures/ && stat --format="%s %n" /e/Projects/corsiriccardo.github.io/assets/fonts/cormorant-garamond-400.woff2 /e/Projects/corsiriccardo.github.io/assets/fonts/cormorant-garamond-600.woff2 /e/Projects/corsiriccardo.github.io/assets/fonts/lora-400.woff2</automated>
  </verify>
  <acceptance_criteria>
    - assets/fonts/cormorant-garamond-400.woff2 exists and is > 10000 bytes (expected ~22876)
    - assets/fonts/cormorant-garamond-600.woff2 exists and is > 10000 bytes (expected ~23396)
    - assets/fonts/lora-400.woff2 exists and is > 10000 bytes (expected ~21148)
    - assets/textures/paper-grain.png exists and is > 1000 bytes
    - All four filenames are lowercase with hyphens — no uppercase characters
    - scripts/download-fonts.js exists
    - scripts/generate-texture.js exists
  </acceptance_criteria>
  <done>Three WOFF2 files downloaded from Google Fonts CDN; paper-grain.png generated. All files in correct locations with correct lowercase names.</done>
</task>

<task type="auto">
  <name>Task 2: Generate OG image and favicon assets</name>
  <files>scripts/generate-og-image.js, assets/og-image.png, assets/favicon.svg, assets/favicon.ico</files>
  <read_first>
    - .planning/phases/01-foundation-shell/01-RESEARCH.md (sections "OG Image: How to Generate" and "Favicon: How to Generate" — contain complete scripts and SVG code)
    - .planning/phases/01-foundation-shell/01-UI-SPEC.md (Open Graph & Meta Contract — exact OG image copy)
  </read_first>
  <action>
Generate OG image using pureimage, write favicon.svg by hand, convert to favicon.ico.

**Step 1 — Install pureimage (dev only):**
```bash
cd /e/Projects/corsiriccardo.github.io && npm install pureimage
```
Verify the install succeeded (pureimage should appear in node_modules/).

**Step 2 — scripts/generate-og-image.js:**
Copy the complete script from RESEARCH.md "OG Image: How to Generate" section with these exact values:
  - Canvas: 1200x630px
  - Background fill: #F5F0E8
  - Main text: "Riccardo Corsi" centered at (600, 280), color #2C2416, bold large serif
  - Subtitle: "Software Developer" centered at (600, 360), color #6B5D4F, smaller serif
  - Accent line: stroke #8B6914, 2px, from (480, 400) to (720, 400)
  - Output: assets/og-image.png

Run it:
```bash
cd /e/Projects/corsiriccardo.github.io && node scripts/generate-og-image.js
```

**Step 3 — assets/favicon.svg:**
Write this file by hand (do not use a script):
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="4" fill="#F5F0E8"/>
  <text x="16" y="23" font-family="Georgia, serif" font-size="20"
        font-weight="600" text-anchor="middle" fill="#2C2416">RC</text>
</svg>
```

**Step 4 — assets/favicon.ico:**
Try npx svg-to-ico first:
```bash
cd /e/Projects/corsiriccardo.github.io && npx svg-to-ico assets/favicon.svg assets/favicon.ico
```
If that fails (tool unavailable), try:
```bash
cd /e/Projects/corsiriccardo.github.io && npx -y to-ico assets/favicon.svg > assets/favicon.ico
```
If both CLI approaches fail: this becomes a checkpoint. The plan has a checkpoint task below that handles the manual fallback.

IMPORTANT: OG image URL in HTML must be absolute: `https://corsiriccardo.github.io/assets/og-image.png` — social crawlers cannot resolve relative paths.
  </action>
  <verify>
    <automated>ls -la /e/Projects/corsiriccardo.github.io/assets/og-image.png /e/Projects/corsiriccardo.github.io/assets/favicon.svg /e/Projects/corsiriccardo.github.io/assets/favicon.ico 2>/dev/null || echo "MISSING FILES"</automated>
  </verify>
  <acceptance_criteria>
    - assets/og-image.png exists and is > 5000 bytes
    - assets/favicon.svg exists and contains the text "RC"
    - assets/favicon.svg contains `fill="#F5F0E8"` (cream background)
    - assets/favicon.svg contains `fill="#2C2416"` (dark text)
    - assets/favicon.ico exists and is > 0 bytes
    - scripts/generate-og-image.js exists
    - All filenames are lowercase
  </acceptance_criteria>
  <done>OG image (1200x630 warm cream with "Riccardo Corsi / Software Developer" text) generated; favicon.svg handwritten with RC initials; favicon.ico converted from SVG.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three WOFF2 font files downloaded from Google Fonts CDN, paper grain PNG generated (200x200px warm noise), OG image PNG generated (1200x630px warm cream), favicon.svg and favicon.ico created. All filenames lowercase.
  </what-built>
  <how-to-verify>
    1. Open a file explorer and navigate to the assets/ directory. Verify this structure:
       - assets/fonts/cormorant-garamond-400.woff2 (~22 KB)
       - assets/fonts/cormorant-garamond-600.woff2 (~23 KB)
       - assets/fonts/lora-400.woff2 (~21 KB)
       - assets/textures/paper-grain.png (small file, ~10-50 KB)
       - assets/og-image.png (larger file, > 5 KB)
       - assets/favicon.svg
       - assets/favicon.ico
    2. Open assets/og-image.png in an image viewer — it should be 1200x630, warm cream background, with "Riccardo Corsi" and "Software Developer" text and a gold accent line.
    3. Open assets/favicon.svg in a browser — it should show a small cream square with "RC" in dark text.
    4. If favicon.ico is 0 bytes or missing: go to https://realfavicongenerator.net, upload assets/favicon.svg, download the favicon.ico, save it as assets/favicon.ico.
    5. Confirm no uppercase letters in any filename under assets/:
       In bash: `ls -R /e/Projects/corsiriccardo.github.io/assets/ | grep -E '[A-Z]'` (should produce no output)
  </how-to-verify>
  <resume-signal>Type "approved" if all assets look correct, or describe any issues (e.g., "og image is blank", "favicon.ico missing")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → local scripts | Font download from fonts.gstatic.com (Google CDN) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Font download from Google CDN | accept | HTTPS enforced; these are public font files; hash verification is overkill for a personal portfolio |
| T-02-02 | Information Disclosure | scripts/ directory | accept | Scripts are dev-only and contain no secrets; they download from public URLs |
</threat_model>

<verification>
After checkpoint approval:

```bash
# Confirm all 7 asset files present
ls /e/Projects/corsiriccardo.github.io/assets/fonts/
ls /e/Projects/corsiriccardo.github.io/assets/textures/
ls /e/Projects/corsiriccardo.github.io/assets/og-image.png
ls /e/Projects/corsiriccardo.github.io/assets/favicon.*

# Confirm no uppercase filenames (LAYOUT-04)
ls -R /e/Projects/corsiriccardo.github.io/assets/ | grep -E '[A-Z]'
```

Expected: 3 WOFF2 files, paper-grain.png, og-image.png, favicon.svg, favicon.ico — all lowercase.
</verification>

<success_criteria>
- assets/fonts/ contains cormorant-garamond-400.woff2 (>10KB), cormorant-garamond-600.woff2 (>10KB), lora-400.woff2 (>10KB)
- assets/textures/paper-grain.png exists and is a valid PNG
- assets/og-image.png is 1200x630px warm cream image with legible "Riccardo Corsi" text
- assets/favicon.svg contains "RC" initials on cream background
- assets/favicon.ico is a valid multi-resolution ICO file
- Zero uppercase characters in any filename under assets/
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md`
</output>
