# Phase 2 — Wave 1B: About Section

**Phase:** 2 — Core Content
**Wave:** 1B — parallel with 1A, 1C, 1D (no shared files)
**Depends on:** 02-01-PLAN.md
**Requirements:** ABOUT-01

## Goal

Fill the `#about` section in `index.html` with exact locked prose and stack groups, and add About CSS (including the shared `.pill` component) to `css/components.css`.

---

## Tasks

### Task 1: Fill #about section in index.html

**Action:** Edit `index.html`. Replace the HTML comment inside `<section id="about" aria-label="About">` (currently: `<!-- Phase 2 content: about copy, stack -->`) with the following content. Do NOT modify the section tag, its `id`, or its `aria-label`. Do NOT touch any other section.

Replace with:

```html
      <h2>About</h2>
      <div class="about__content">
        <p class="about__prose">I build tools for game developers — systems that bridge engine internals with production realities in Unreal and Unity. I care about correctness under pressure: code that performs reliably whether running in a game loop or a build pipeline. Currently focused on cross-engine tooling and backend infrastructure for game projects.</p>
        <dl class="about__stack">
          <div class="about__stack-group">
            <dt>Languages</dt>
            <dd>
              <ul class="about__pills">
                <li><span class="pill">C++</span></li>
                <li><span class="pill">C#</span></li>
                <li><span class="pill">Python</span></li>
                <li><span class="pill">JavaScript</span></li>
              </ul>
            </dd>
          </div>
          <div class="about__stack-group">
            <dt>Engines</dt>
            <dd>
              <ul class="about__pills">
                <li><span class="pill">Unreal Engine</span></li>
                <li><span class="pill">Unity</span></li>
              </ul>
            </dd>
          </div>
          <div class="about__stack-group">
            <dt>Tools</dt>
            <dd>
              <ul class="about__pills">
                <li><span class="pill">Git</span></li>
                <li><span class="pill">Rider</span></li>
                <li><span class="pill">VS Code</span></li>
              </ul>
            </dd>
          </div>
        </dl>
      </div>
```

Copy constraints:
- Prose text is EXACTLY: `I build tools for game developers — systems that bridge engine internals with production realities in Unreal and Unity. I care about correctness under pressure: code that performs reliably whether running in a game loop or a build pipeline. Currently focused on cross-engine tooling and backend infrastructure for game projects.`
- Stack group labels are EXACTLY: `Languages`, `Engines`, `Tools`
- Languages pills: `C++`, `C#`, `Python`, `JavaScript` (exactly these 4 — D-12)
- Engines pills: `Unreal Engine`, `Unity` (exactly these 2 — D-12)
- Tools pills: `Git`, `Rider`, `VS Code` (exactly these 3 — D-12)
- Use `<dl>` for the stack (definition list — labels are terms, pill groups are definitions)
- Use `<ul class="about__pills">` inside each `<dd>` — list of pills, no list markers
- Each pill is `<span class="pill">` inside an `<li>` — do NOT put `.pill` directly on the `<li>`
- No progress bars, no percentage numbers, no skill level indicators

**Read first:**
- `index.html` — read the full file to locate the `#about` placeholder comment and confirm surrounding context before editing

**Acceptance criteria:**
- `grep "class=\"about__prose\"" index.html` returns a match
- `grep "about__stack" index.html` returns a match
- `grep -c "about__stack-group" index.html` outputs `3` (one per stack group)
- `grep "about__pills" index.html` returns a match
- `grep "class=\"pill\">C++" index.html` returns a match
- `grep "JavaScript" index.html` returns a match inside `#about` (D-12 language)
- `grep "Unreal Engine" index.html` returns a match inside `#about`
- `grep "Rider" index.html` returns a match inside `#about` (D-12 tool)
- `grep "VS Code" index.html` returns a match inside `#about` (D-12 tool)
- `grep "progress" index.html` returns no matches (no progress bars)

**Requirement IDs:** ABOUT-01

---

### Task 2: Add About CSS and shared pill CSS to css/components.css

**Action:** Edit `css/components.css`. The file has exactly one `@layer components { }` block. After Plan 02-02 executes, the block will contain nav CSS and hero/button CSS. Append the following block INSIDE the existing `@layer components { }` — inserted immediately before the single closing `}`. Do NOT open a second `@layer components { }` block.

Append this exact block:

```css

  /* ===== Phase 2: Pill (shared — about stack + project card tech tags) ===== */
  .pill {
    display: inline-block;
    font-family: var(--font-family-body);
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-regular);
    line-height: var(--line-height-label);
    color: var(--color-text-secondary);
    /* rgba() fallback before color-mix() */
    background-color: rgba(44, 36, 22, 0.08);
    background-color: color-mix(in srgb, var(--color-text-primary) 8%, transparent);
    /* rgba() fallback before color-mix() */
    border: 1px solid rgba(44, 36, 22, 0.15);
    border: 1px solid color-mix(in srgb, var(--color-text-primary) 15%, transparent);
    padding: var(--space-xs) var(--space-sm);
    border-radius: 2px;
  }

  /* ===== Phase 2: About ===== */
  #about h2 {
    margin-bottom: var(--space-xl);
  }

  /* Single column on mobile; prose left + stack right on wide screens */
  .about__content {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
  }

  @media (min-width: 1024px) {
    .about__content {
      grid-template-columns: 3fr 2fr;
      gap: var(--space-3xl);
    }
  }

  .about__prose {
    font-family: var(--font-family-body);
    font-size: var(--font-size-body);
    font-weight: var(--font-weight-regular);
    line-height: var(--line-height-body);
    color: var(--color-text-primary);
    max-width: 65ch;
    margin: 0;
  }

  /* Definition list: Labels column + pills column */
  .about__stack {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    margin: 0;
  }

  .about__stack-group {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xs);
  }

  @media (min-width: 768px) {
    .about__stack-group {
      grid-template-columns: 100px 1fr;
      align-items: start;
      gap: var(--space-md);
    }
  }

  /* Label styling: uppercase small caps, secondary color */
  .about__stack-group dt {
    font-family: var(--font-family-body);
    font-size: var(--font-size-label);
    font-weight: var(--font-weight-regular);
    line-height: var(--line-height-label);
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding-top: var(--space-xs);
  }

  .about__stack-group dd {
    margin: 0;
  }

  /* Pill list: flex row, wraps, no list markers */
  .about__pills {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    list-style: none;
    padding: 0;
    margin: 0;
  }
```

Constraints:
- `.pill` is defined here because About is the first section that uses it. Project cards (Plan 05) reuse it without redeclaring.
- Each `color-mix()` line must be immediately preceded by its `rgba()` fallback on the previous line — two separate property declarations, same property name.
- `letter-spacing: 0.08em` on `dt` — this is an `em`-relative value, not a token. It is a deliberate typographic choice for the label small-caps effect.
- `grid-template-columns: 100px 1fr` on `.about__stack-group` at 768px — the `100px` fixed label column width is a deliberate layout value, not a spacing token.
- No `@media (prefers-reduced-motion: reduce)` needed — this CSS block has no transitions or transforms.
- Count of `@layer components` blocks in the file must remain `1`.

**Read first:**
- `css/components.css` — read the full file to locate the closing `}` and confirm what CSS already exists (from Plan 02-02, hero and button CSS will be present)

**Acceptance criteria:**
- `grep "\.pill" css/components.css` returns a match with `color-mix(` on the background property
- `grep "about__prose" css/components.css` returns a match with `max-width: 65ch`
- `grep "about__stack" css/components.css` returns a match
- `grep "letter-spacing" css/components.css` returns a match with `0.08em`
- `grep "text-transform: uppercase" css/components.css` returns a match on `dt`
- `grep "about__pills" css/components.css` returns a match with `list-style: none`
- `grep -c "@layer components" css/components.css` outputs `1`

**Requirement IDs:** ABOUT-01

---
